import {PATHS} from "../../constants/path"

export const authenticationsRoutes = [{
    path: PATHS.USERS,
    // element: <s />,
    // layout: AdminLayout,
    // roles: [ROLES.ADMIN],
    // permissions: [PERMISSIONS.USER_VIEW],
    // menu: true,
}];